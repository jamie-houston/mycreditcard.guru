# Production Environment Variables for PythonAnywhere
# Copy this file to .env and update with your actual values

# SECURITY WARNING: Generate a new secret key for production!
# Use: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your-super-secret-production-key-at-least-50-characters-long

# Production settings
DEBUG=True

# Replace 'yourusername' with your actual PythonAnywhere username
# Add any custom domains you've configured
ALLOWED_HOSTS=localhost,127.0.0.1,testserver

# Database Configuration
# Option 1: SQLite (default, good for small apps)
DATABASE_URL=sqlite:///db.sqlite3

# Option 2: MySQL (recommended for production)
# Create database from PythonAnywhere dashboard first
# DATABASE_URL=mysql://yourusername:password@yourusername.mysql.pythonanywhere-services.com/yourusername$creditcard_guru

# Google OAuth Settings (Optional)
# Get these from Google Cloud Console
# https://console.cloud.google.com
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=

# CORS Settings
# Include all domains that will access your API
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Email Settings (Optional - for password reset, etc.)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password

# Additional Security Settings (Optional)
# SECURE_PROXY_SSL_HEADER=HTTP_X_FORWARDED_PROTO,https